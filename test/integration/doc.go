// Package integration_test contains runtime and container integration tests.
// This non-test file intentionally keeps the package discoverable by ./... so
// the standard gate cannot silently omit Testcontainers coverage.
package integration_test
