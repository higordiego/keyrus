# Loaded automatically by `docker compose` from the repo root. Sets which
# profiles are active by default so a bare `docker compose up` (no --profile
# flags) still brings up the full business stack, matching T10's acceptance
# criterion, while `docker compose --profile observability up` or
# `--profile load-test run k6` opt into the heavier/optional layers.
COMPOSE_PROFILES=app
