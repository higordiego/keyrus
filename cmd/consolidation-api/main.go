// Command consolidation-api is the future Consolidation process entrypoint.
// T01 intentionally provides only a compilable contract foundation.
package main

import "log"

func main() {
	log.Print("consolidation-api: contract foundation only; runtime is introduced by a later ticket")
}
