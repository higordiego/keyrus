# One @SCN-* tag per line. Only tags in this manifest are executed by Godog.
# Adding a tag without a real binding makes the strict Godog suite fail.
#
# T02's previous bindings used in-process substitutes for KrakenD, Keycloak and
# the protected adapters. They remain useful unit/contract fixtures, but are no
# longer presented as implemented BDD evidence. The same security properties
# are exercised against the real containerized runtime in
# test/integration/runtime_e2e_test.go. A scenario may return to this manifest
# only when its Godog binding drives that real runtime.
